# =========================================================
# RAG FRONTEND - HTTP
# =========================================================

server {
    listen 80;
    server_name rag.ranjandev.tech;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}


# =========================================================
# RAG FRONTEND - HTTPS
# =========================================================

server {
    listen 443 ssl;
    server_name rag.ranjandev.tech;

    ssl_certificate /etc/letsencrypt/live/rag.ranjandev.tech/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/rag.ranjandev.tech/privkey.pem;

    location / {
        proxy_pass http://client:3000;

        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}


# RAG API - HTTP
server {
    listen 80;
    server_name api.rag.ranjandev.tech;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}


# RAG API - HTTPS
server {
    listen 443 ssl;
    server_name api.rag.ranjandev.tech;

    ssl_certificate /etc/letsencrypt/live/api.rag.ranjandev.tech/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.rag.ranjandev.tech/privkey.pem;

    location / {
        proxy_pass http://server:8000;

        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}